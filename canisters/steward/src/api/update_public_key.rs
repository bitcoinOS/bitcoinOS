// use crate::{ICBitcoinNetwork, PublicKey};

// pub async fn serve(
//     network: ICBitcoinNetwork,
//     derivation: Vec<Vec<u8>>,
// ) -> PublicKey {
//     base::ecdsa_public_key(get_key_name(network), derivation).await
// }
